# Contributing

Keep the native plugin, CLI, MCP server, and Playwright fixture on one JSON-RPC contract. A feature is incomplete until the protocol, all public clients, and the real multi-window fixture agree.

Do not add silent fallbacks. Missing applications, windows, selectors, transports, or capabilities must fail with an actionable error.
