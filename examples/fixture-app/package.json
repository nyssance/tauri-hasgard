{
  "name": "tauri-hasgard-fixture-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 14731 --strictPort",
    "build": "tsc --noEmit && vite build",
    "build:e2e": "cargo build -p tauri-hasgard-cli && tauri build --debug --no-bundle --features hasgard-testing",
    "typecheck": "tsc --noEmit",
    "tauri": "tauri",
    "test:e2e": "bun run build:e2e && playwright test --config e2e/playwright.config.ts",
    "test:e2e:stress": "bun run build:e2e && HASGARD_NATIVE_STRESS=1 playwright test --config e2e/playwright.config.ts native-focus.spec.ts"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  },
  "devDependencies": {
    "@nyssance/tauri-hasgard": "workspace:*",
    "@playwright/test": "^1.61.1",
    "@tauri-apps/cli": "^2.11.4",
    "@types/node": "^26.1.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.0"
  }
}
