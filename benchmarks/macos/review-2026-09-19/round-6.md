# 第 6 轮 · Claude 评审

Session: `37cbf695-d473-47af-bbde-069c3c9a0fb0`

## Codex 提交讨论的问题

第6轮还不能给最终100：最新来源/退出修复提交bd3bf9b的mac原生E2E已通过，Linux/Windows原生仍运行很久，旧任务已取消取日志诊断。本地最终Rust360、TS131、bridge126通过。所有可实施功能缺项已补齐，但必须解决当前平台验证再标100。补CI maxFailures=3和job30min，防止入口失败反复整套运行。补fixture启动失败的进程诊断到CI日志，视频测试明确附captured:true实际产物或captured:false权限错误，不能仅用测试通过推断视频捕获成功。
请针对这个完成门槛和记录方式确认是否一致，并检查现有方案是否还有盲点。注意不把远程正在运行写成通过，不把历史本机未复测写成未重现。之后有新CI证据再追加最终共识轮，无工具。

## Claude 回复

**完成门槛：一致。** 提交 bd3bf9b 的 mac 原生 E2E 已通过，Linux 与 Windows 原生仍在运行，本轮不标 100，等平台验证完成后追加最终共识轮。本地 Rust 360、TS 131、bridge 126 通过只作为本地证据记录。

**记录方式：一致，并补三点措辞约束。**

- 远程状态只写“运行中”或“已取消取日志”，不写通过。
- keyboard 写“本轮未复测，历史未归因记录保留”。
- 视频结果以 `captured:true` 加实际产物路径与字节数，或 `captured:false` 加权限错误原文为准，测试通过本身不作为捕获成功证据。

**CI 防护：同意** maxFailures=3 与 job 30 分钟上限，以及 fixture 启动失败时把进程诊断写入 CI 日志。

**剩余盲点，需在最终轮前确认：**

1. **Linux 运行很久的原因未归因。** 取消取日志是对的，但要区分是握手改动在 webkit2gtk 上的真实失败，还是 runner 资源慢。归因结果应进最终记录，否则“Linux 通过”缺少解释力。
2. **`captured:true` 的产物校验深度。** 建议记录帧数与时长，而不只是非零字节，否则一段只有首帧的 mp4 也会被算作成功。
3. **Windows required 后首次运行的基线。** 从 allow_failure 改为 required 后，第一次完整通过应单独记为基线，方便之后区分回归与环境抖动。
4. **性能数字来源。** 最终记录里每个百分比都要带来源 commit 与日期，明确未重跑。
5. **分母声明。** 最终 HTML 顶部一句话写明“100% 指五轮以上讨论达成的共识，范围与边界见下”，避免读者误读为矩阵覆盖率。

以上五项都是记录与归因工作，不是实现缺口。新 CI 证据到达后即可进入最终轮。
